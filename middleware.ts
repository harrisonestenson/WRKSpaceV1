import { NextResponse } from 'next/server'
import type { NextRequest } from 'next/server'

export function middleware(request: NextRequest) {
  const { pathname } = request.nextUrl

  // Allow access to role selection and onboarding without authentication
  if (pathname === '/role-select' || pathname.startsWith('/onboarding')) {
    return NextResponse.next()
  }

  // For all other pages, check if user is authenticated
  // For now, we'll allow access to everything to test the functionality
  return NextResponse.next()
}

export const config = {
  matcher: [
    /*
     * Match all request paths except for the ones starting with:
     * - api (API routes)
     * - _next/static (static files)
     * - _next/image (image optimization files)
     * - favicon.ico (favicon file)
     */
    '/((?!api|_next/static|_next/image|favicon.ico).*)',
  ],
} 